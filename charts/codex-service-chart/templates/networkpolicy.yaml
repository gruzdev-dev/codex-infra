{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "codex-service-chart.fullname" . }}
  labels:
    {{- include "codex-service-chart.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "codex-service-chart.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
  {{- toYaml .Values.networkPolicy.ingress | nindent 4 }}
{{- end }}
