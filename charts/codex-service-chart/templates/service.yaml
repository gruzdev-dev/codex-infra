apiVersion: v1
kind: Service
metadata:
  name: {{ include "codex-service-chart.fullname" . }}
  labels:
    {{- include "codex-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codex-service-chart.selectorLabels" . | nindent 4 }}
